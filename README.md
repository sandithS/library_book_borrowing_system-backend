# 📚 Library Book Borrowing System - Backend

This is the backend repository for the Library Book Borrowing System. It is built using **Spring Boot** and **Java**, designed to handle all the core business logic, data management, and API endpoints for a library application.

## 🛠️ Technology Stack

* **Language:** Java (JDK 22)
* **Framework:** Spring Boot (Version 4.0.0-M2)
* **Build Tool:** Maven
* **Dependencies:**
    * `spring-boot-starter-web`: Provides all the necessary components for building web, including RESTful, applications.

## ⚙️ Project Setup

### Prerequisites

* **Java Development Kit (JDK) 22**
* **Maven**
* An Integrated Development Environment (IDE) like IntelliJ IDEA or Eclipse.

### Cloning the Repository

```bash
git clone <repository-url>
cd Library_Book_Borrowing_System
